pipeline {
    agent any  // Run on any available agent

    stages {
        stage('Hello') {
            steps {
                // Print to Jenkins console log
                echo 'Hello World'
            }
        }
    }
}